{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "functions"
  ],
  "scripts": {
    "dev": "yarn workspace client dev",
    "build": "yarn workspace client build",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "papaparse": "^5.5.2",
    "zod": "^3.24.2"
  }
}
